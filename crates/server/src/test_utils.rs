#[cfg(test)]
mod test_utils {
    use std::net::TcpStream;

    // fn tcp_stream() -> TcpStream {
    //     let listener = std::net::TcpListener::bind("127.0.0.1:0").unwrap();

    //     let addr = listener.local_addr();

    // }
}
